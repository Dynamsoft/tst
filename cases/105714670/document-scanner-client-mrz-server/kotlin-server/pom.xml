<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.dynamsoft.sample</groupId>
  <artifactId>mds-mrz-kotlin-server</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>
  <name>Document Scanner Client + Server-side MRZ - Kotlin Backend</name>
  <description>Serves the React Mobile Document Scanner client and performs server-side MRZ reading with Dynamsoft Capture Vision Java Edition</description>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <properties>
    <java.version>21</java.version>
    <kotlin.version>2.4.10</kotlin.version>
    <dynamsoft.dcv.version>3.6.1000</dynamsoft.dcv.version>
  </properties>

  <repositories>
    <!-- Dynamsoft Capture Vision Java Edition is NOT on Maven Central. -->
    <repository>
      <id>dynamsoft</id>
      <url>https://download2.dynamsoft.com/maven/jar</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.module</groupId>
      <artifactId>jackson-module-kotlin</artifactId>
    </dependency>
    <dependency>
      <groupId>org.jetbrains.kotlin</groupId>
      <artifactId>kotlin-stdlib</artifactId>
    </dependency>
    <dependency>
      <groupId>org.jetbrains.kotlin</groupId>
      <artifactId>kotlin-reflect</artifactId>
    </dependency>

    <!-- Dynamsoft Capture Vision Java Edition: barcode, label recognizer,
         code parser and the native engine libraries in a single artifact. -->
    <dependency>
      <groupId>com.dynamsoft</groupId>
      <artifactId>dcv</artifactId>
      <version>${dynamsoft.dcv.version}</version>
    </dependency>
  </dependencies>

  <build>
    <sourceDirectory>${project.basedir}/src/main/kotlin</sourceDirectory>
    <plugins>
      <plugin>
        <groupId>org.jetbrains.kotlin</groupId>
        <artifactId>kotlin-maven-plugin</artifactId>
        <configuration>
          <args>
            <arg>-Xjsr305=strict</arg>
          </args>
          <compilerPlugins>
            <plugin>spring</plugin>
          </compilerPlugins>
          <jvmTarget>${java.version}</jvmTarget>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-maven-allopen</artifactId>
            <version>${kotlin.version}</version>
          </dependency>
        </dependencies>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
